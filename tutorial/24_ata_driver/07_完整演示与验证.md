# 完整演示与验证 —— Stage 24 ATA 驱动实战指南

## 前言

经过前面六篇文章的努力，我们已经实现了一个完整的 ATA 磁盘驱动。从设备检测到 IDENTIFY 命令，从 PIO 读写到 LBA48 扩展，再到异步 I/O，每一步都是我们亲手完成的。

说实话，写驱动最让人兴奋的时刻就是看到它真正工作的时候。之前所有的抽象代码、所有的寄存器操作，在这一刻都变成了实实在在的数据传输。这篇文章我们会通过演示程序来验证我们实现的每一个功能。

准备好了吗？让我们来见证成果。

---

## 演示程序概述

我们的演示程序包含了 7 个测试用例，覆盖了驱动的所有核心功能。

### 测试列表

```
1. MBR 读取与验证
2. 设备信息显示
3. 多扇区读取
4. 写入与回读验证（安全测试）
5. 性能测试
6. LBA48 支持测试
7. 异步 I/O 测试
```

### 运行演示

在内核初始化后调用：

```c
// 内核初始化
void kernel_init(void) {
    // 初始化 ATA 驱动
    int result = ata_init();
    if (result != 0) {
        klog_error("Failed to initialize ATA driver\n");
        return;
    }

    // 运行演示程序
    ata_run_demo();
}
```

---

## 演示 1：MBR 读取与验证

MBR（主引导记录）位于磁盘的第一个扇区（LBA 0），包含了磁盘的分区表信息。

### 实现代码

```c
static void demo_read_mbr(void) {
    klog_trace("\n=== Demo 1: Reading MBR ===\n");

    uint8_t mbr_buffer[ATA_SECTOR_SIZE];
    ata_result_t result = ata_read(ATA_PRIMARY_MASTER, 0, mbr_buffer, 1);

    if (result != ATA_OK) {
        klog_error("[ATA Demo] Failed to read MBR: %s\n",
                   ata_error_string(result));
        return;
    }

    klog_info("Successfully read MBR (512 bytes)\n");

    // 检查 MBR 签名（最后两个字节）
    uint16_t signature = *(uint16_t*)&mbr_buffer[510];
    if (signature == MBR_SIGNATURE) {
        klog_info("MBR signature valid: 0x%04X\n", signature);
    } else {
        klog_warn("MBR signature invalid: 0x%04X (expected 0x%04X)\n",
                  signature, MBR_SIGNATURE);
    }

    // 显示分区表
    klog_trace("Partition Table:\n");
    for (int i = 0; i < 4; i++) {
        uint8_t* entry = &mbr_buffer[446 + i * 16];
        uint8_t status = entry[0];
        uint8_t type = entry[4];

        if (type != 0) {
            uint32_t lba = *(uint32_t*)&entry[8];
            uint32_t sectors = *(uint32_t*)&entry[12];

            klog_trace("  Partition %d: Type=0x%02X, Status=0x%02X, "
                      "LBA=%u, Sectors=%u\n",
                      i, type, status, lba, sectors);
        } else {
            klog_trace("  Partition %d: Empty\n", i);
        }
    }
}
```

### 预期输出

```
=== Demo 1: Reading MBR ===
Successfully read MBR (512 bytes)
MBR signature valid: 0xAA55
Partition Table:
  Partition 0: Type=0x83, Status=0x80, LBA=2048, Sectors=2097152
  Partition 1: Type=0x82, Status=0x00, LBA=2099200, Sectors=1048576
  Partition 2: Empty
  Partition 3: Empty
```

### 验证要点

1. **成功读取** - 能读取 512 字节
2. **签名正确** - 最后两个字节是 0xAA55
3. **分区表解析** - 能正确显示分区信息

---

## 演示 2：设备信息显示

这个演示展示了我们从 IDENTIFY 命令中获取的所有设备信息。

### 实现代码

```c
static void demo_device_info(void) {
    klog_trace("\n=== Demo 2: Device Information ===\n");

    for (int dev = 0; dev < 4; dev++) {
        if (!ata_device_exists(dev)) {
            continue;
        }

        const char* ctrl_name = (dev < 2) ? "Primary" : "Secondary";
        const char* dev_name = (dev % 2 == 0) ? "Master" : "Slave";

        klog_trace("--- %s %s ---\n", ctrl_name, dev_name);

        ata_device_info_t info;
        ata_result_t result = ata_get_info(dev, &info);

        if (result == ATA_OK) {
            klog_trace("  Model:    %s\n", info.model);
            klog_trace("  Serial:   %s\n", info.serial);
            klog_trace("  Firmware: %s\n", info.firmware);
            klog_trace("  LBA:      %s\n", info.lba_supported ? "Yes" : "No");
            klog_trace("  LBA48:    %s\n", info.lba48_supported ? "Yes" : "No");
            klog_trace("  DMA:      %s\n", info.dma_supported ? "Yes" : "No");

            uint64_t sectors = ata_get_sector_count(dev);
            uint64_t bytes = sectors * ATA_SECTOR_SIZE;
            uint64_t mb = bytes / (1024 * 1024);

            klog_info("  Capacity: %llu sectors (%llu MB, %llu GB)\n",
                     sectors, mb, mb / 1024);
        }
    }
}
```

### 预期输出

```
=== Demo 2: Device Information ===
--- Primary Master ---
  Model:    QEMU HARDDISK
  Serial:   QM00001
  Firmware: 2.5+
  LBA:      Yes
  LBA48:    Yes
  DMA:      No
  Capacity: 262144 sectors (128 MB, 0 GB)
```

### 验证要点

1. **型号显示正确** - 不是乱码
2. **LBA/LBA48 支持** - 能力位解析正确
3. **容量计算正确** - 与 QEMU 配置一致

---

## 演示 3：多扇区读取

这个演示测试读取多个连续扇区的能力。

### 实现代码

```c
static void demo_read_multiple(void) {
    klog_trace("\n=== Demo 3: Reading Multiple Sectors ===\n");

    uint32_t lba = TEST_SECTOR_LBA;  // 扇区 1（避开 MBR）
    uint16_t sectors = TEST_SECTOR_COUNT;  // 2 个扇区

    klog_trace("Reading %u sectors starting from LBA %u...\n", sectors, lba);

    uint8_t buffer[sectors * ATA_SECTOR_SIZE];
    ata_result_t result = ata_read(ATA_PRIMARY_MASTER, lba, buffer, sectors);

    if (result != ATA_OK) {
        klog_error("[ATA Demo] Failed to read sectors: %s\n",
                   ata_error_string(result));
        return;
    }

    klog_info("Successfully read %u sectors (%u bytes)\n",
              sectors, sectors * ATA_SECTOR_SIZE);

    // 显示第一个扇区的前 64 字节
    klog_trace("Hex dump of first sector (first 64 bytes):\n");
    for (int row = 0; row < 4; row++) {
        klog_trace("  0x%03X: ", row * 16);
        for (int col = 0; col < 16; col++) {
            klog_trace("%02X ", buffer[row * 16 + col]);
        }
        klog_trace(" | ");
        // ASCII 显示
        for (int col = 0; col < 16; col++) {
            uint8_t c = buffer[row * 16 + col];
            klog_trace("%c", (c >= 32 && c < 127) ? c : '.');
        }
        klog_trace("\n");
    }
}
```

### 预期输出

```
=== Demo 3: Reading Multiple Sectors ===
Reading 2 sectors starting from LBA 1...
Successfully read 2 sectors (1024 bytes)
Hex dump of first sector (first 64 bytes):
  0x000: 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00  | ................
  0x010: 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00  | ................
  0x020: 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00  | ................
  0x030: 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00  | ................
```

### 验证要点

1. **多扇区读取成功** - 能读取 2 个扇区（1024 字节）
2. **数据一致性** - 两次读取相同位置的数据应该一致
3. **十六进制显示** - 格式正确

---

## 演示 4：写入与回读验证

这个演示测试写入功能。为了安全，默认是禁用的。

### 实现代码（简化版）

```c
static void demo_write_verify(void) {
    klog_trace("\n=== Demo 4: Write/Verify Test ===\n");
    klog_warn("This demo will MODIFY sector %d!\n", TEST_SECTOR_LBA);
    klog_trace("Skipping destructive write test for safety.\n");

    // 如果要启用，取消下面的注释：
    /*
    uint32_t lba = TEST_SECTOR_LBA;
    uint8_t original_buffer[ATA_SECTOR_SIZE];
    uint8_t test_pattern[ATA_SECTOR_SIZE];
    uint8_t read_back_buffer[ATA_SECTOR_SIZE];

    // 1. 先读取原始内容
    ata_read(ATA_PRIMARY_MASTER, lba, original_buffer, 1);

    // 2. 创建测试模式
    for (int i = 0; i < ATA_SECTOR_SIZE; i++) {
        switch (i % 4) {
            case 0: test_pattern[i] = 0xAA; break;
            case 1: test_pattern[i] = 0x55; break;
            case 2: test_pattern[i] = 0xFF; break;
            case 3: test_pattern[i] = 0x00; break;
        }
    }

    // 3. 写入测试模式
    ata_write(ATA_PRIMARY_MASTER, lba, test_pattern, 1);

    // 4. 读回验证
    ata_read(ATA_PRIMARY_MASTER, lba, read_back_buffer, 1);

    // 5. 比对数据
    bool match = memcmp(test_pattern, read_back_buffer, ATA_SECTOR_SIZE) == 0;

    if (match) {
        klog_info("Write/Verify: PASSED\n");
    } else {
        klog_error("Write/Verify: FAILED\n");
    }

    // 6. 恢复原始内容
    ata_write(ATA_PRIMARY_MASTER, lba, original_buffer, 1);
    */
}
```

### 验证要点

1. **原始数据保存** - 写入前保存原始内容
2. **测试模式** - 使用可识别的模式
3. **数据比对** - 写入后读回验证
4. **恢复数据** - 测试完成后恢复原始内容

---

## 演示 5：性能测试

这个演示测试读取性能（需要定时器支持）。

### 实现代码

```c
static void demo_performance(void) {
    klog_trace("\n=== Demo 5: Read Performance Test ===\n");

    uint32_t lba = TEST_SECTOR_LBA;
    uint16_t sectors = 16;  // 8KB
    uint8_t buffer[sectors * ATA_SECTOR_SIZE];

    klog_trace("Reading %u sectors (%u KB)...\n",
              sectors, (sectors * ATA_SECTOR_SIZE) / 1024);

    ata_result_t result = ata_read(ATA_PRIMARY_MASTER, lba, buffer, sectors);

    if (result != ATA_OK) {
        klog_error("[ATA Demo] Failed to read: %s\n",
                   ata_error_string(result));
        return;
    }

    klog_info("Read test completed\n");
    klog_trace("Note: Performance timing requires timer integration\n");
}
```

### 性能估算

在 QEMU 中，PIO 模式的读取速度大约是：
- 单扇区：~10-20 MB/s
- 多扇区：~20-30 MB/s

实际速度取决于：
- QEMU 配置
- 主机性能
- 扇区数量

---

## 演示 6：LBA48 支持测试

这个演示验证 LBA48 扩展功能。

### 实现代码

```c
static void demo_lba48_support(void) {
    klog_trace("\n=== Demo 6: LBA48 Support Test ===\n");

    // 检查设备是否支持 LBA48
    ata_device_info_t info;
    if (ata_get_info(ATA_PRIMARY_MASTER, &info) != ATA_OK) {
        klog_error("Failed to get device info\n");
        return;
    }

    klog_info("Device: %s\n", info.model);
    klog_info("LBA48 Supported: %s\n", info.lba48_supported ? "Yes" : "No");

    if (info.lba48_supported) {
        klog_info("LBA48 Sectors: %llu\n", info.lba48_sectors);

        // 测试高 LBA 地址读取
        uint64_t high_lba = info.lba48_sectors - 1;
        if (high_lba > 0xFFFFFFFF) {
            high_lba = 0x10000000;  // LBA28 边界之外
        }

        uint8_t buffer1[ATA_SECTOR_SIZE];
        klog_trace("Attempting LBA48 read at LBA %llu...\n", high_lba);

        ata_result_t result = ata_read(ATA_PRIMARY_MASTER, high_lba, buffer1, 1);
        if (result == ATA_OK) {
            klog_info("LBA48 read successful at high LBA\n");
        } else {
            klog_warn("LBA48 read at high LBA failed: %s\n",
                      ata_error_string(result));
        }
    }

    // 验证 LBA28 兼容性
    uint8_t buffer3[ATA_SECTOR_SIZE];
    klog_trace("Testing LBA28 compatibility (low LBA)...\n");
    result = ata_read(ATA_PRIMARY_MASTER, 100, buffer3, 1);
    if (result == ATA_OK) {
        klog_info("LBA28 auto-selection working correctly\n");
    }
}
```

### 预期输出

```
=== Demo 6: LBA48 Support Test ===
Device: QEMU HARDDISK
LBA48 Supported: Yes
LBA48 Sectors: 262144
Testing LBA28 compatibility (low LBA)...
LBA28 auto-selection working correctly
```

### 验证要点

1. **LBA48 检测** - 能正确识别设备能力
2. **高 LBA 读取** - 能读取超过 LBA28 范围的扇区
3. **LBA28 兼容** - 低 LBA 地址仍使用 LBA28

---

## 演示 7：异步 I/O 测试

这个演示验证中断驱动的异步 I/O 功能。

### 实现代码

```c
static volatile bool g_async_read_complete = false;
static ata_result_t g_async_read_result = ATA_OK;

static void demo_async_read_callback(int device, uint64_t lba, uint16_t sectors,
                                     void* buffer, ata_result_t result, void* context) {
    g_async_read_complete = true;
    g_async_read_result = result;

    klog_info("Async read callback: result=%s\n", ata_error_string(result));
}

static void demo_async_io(void) {
    klog_trace("\n=== Demo 7: Async I/O Test ===\n");

    // 初始化异步模式
    ata_result_t result = ata_init_async_mode();
    if (result != ATA_OK) {
        klog_error("Failed to init async I/O: %s\n", ata_error_string(result));
        return;
    }

    klog_info("Async I/O initialized\n");

    // 测试 1：带回调的异步读取
    uint8_t buffer1[ATA_SECTOR_SIZE];
    g_async_read_complete = false;

    klog_trace("Initiating async read of sector 1...\n");
    result = ata_read_async(ATA_PRIMARY_MASTER, 1, buffer1, 1,
                           demo_async_read_callback, NULL);

    if (result != ATA_OK) {
        klog_error("Failed to queue async read: %s\n",
                   ata_error_string(result));
        return;
    }

    klog_info("Async read queued\n");

    // 等待完成
    uint32_t timeout = 1000000;
    while (!g_async_read_complete && timeout-- > 0) {
        __asm__ volatile("pause");
    }

    if (g_async_read_complete) {
        klog_info("Async read completed successfully\n");
    } else {
        klog_error("Async read timeout\n");
    }

    // 测试 2：多个并发异步读取
    klog_trace("Testing multiple concurrent async reads...\n");

    uint8_t buffers[4][ATA_SECTOR_SIZE];
    uint32_t sectors_test[4] = {10, 20, 30, 40};
    int pending_count = 0;

    for (int i = 0; i < 4; i++) {
        result = ata_read_async(ATA_PRIMARY_MASTER, sectors_test[i],
                               buffers[i], 1, NULL, NULL);
        if (result == ATA_OK) {
            pending_count++;
        }
    }

    klog_info("Queued %d async reads\n", pending_count);

    // 等待全部完成
    timeout = 2000000;
    while (!ata_async_is_idle(ATA_PRIMARY_MASTER) && timeout-- > 0) {
        __asm__ volatile("pause");
    }

    if (ata_async_is_idle(ATA_PRIMARY_MASTER)) {
        klog_info("All async reads completed\n");
    }
}
```

### 预期输出

```
=== Demo 7: Async I/O Test ===
Async I/O initialized
Initiating async read of sector 1...
Async read queued
Async read callback: result=OK
Async read completed successfully
Testing multiple concurrent async reads...
Queued 4 async reads
All async reads completed
```

### 验证要点

1. **异步初始化** - IRQ 注册成功
2. **回调触发** - 操作完成后回调被调用
3. **队列管理** - 多个操作能正确排队
4. **并发处理** - 多个操作能依次完成

---

## QEMU 调试技巧

在开发过程中，QEMU 提供了一些有用的调试功能。

### 创建测试磁盘

```bash
# 创建原始磁盘镜像
dd if=/dev/zero of=disk.img bs=1M count=128

# 或创建 qcow2 镜像
qemu-img create -f qcow2 disk.qcow2 128M

# 写入一些测试数据
dd if=/dev/urandom of=disk.img bs=512 count=1 conv=notrunc
```

### QEMU Monitor 命令

在 QEMU 运行时按 Ctrl+Alt+2（或使用 `-monitor stdio`）进入 monitor：

```
# 查看块设备信息
info block

# 查看块设备统计
info blockstats

# 弹出磁盘
eject ide0-hd0

# 插入新磁盘
change ide0-hd0 new_disk.img
```

### GDB 调试

```bash
# 终端 1：启动 QEMU
qemu-system-x86_64 -kernel kernel.elf -s -S

# 终端 2：启动 GDB
gdb kernel.elf
(gdb) target remote :1234
(gdb) break ata_read_pio
(gdb) continue
```

---

## 完整测试流程

### 编译运行

```bash
# 编译
cd build
make -j$(nproc)

# 运行 QEMU
make run
```

### 预期完整输出

```
========================================
   ATA/IDE Disk Driver Demo
========================================

[ATA] Initializing ATA driver...
[ATA] Primary Master: Present
[ATA] Primary Slave: Not Present
[ATA] Secondary Master: Not Present
[ATA] Secondary Slave: Not Present

=== Demo 1: Reading MBR ===
Successfully read MBR (512 bytes)
MBR signature valid: 0xAA55
Partition Table:
  Partition 0: Empty
  Partition 1: Empty
  Partition 2: Empty
  Partition 3: Empty

=== Demo 2: Device Information ===
--- Primary Master ---
  Model:    QEMU HARDDISK
  Serial:   QM00001
  Firmware: 2.5+
  LBA:      Yes
  LBA48:    Yes
  DMA:      No
  Capacity: 262144 sectors (128 MB, 0 GB)

=== Demo 3: Reading Multiple Sectors ===
Reading 2 sectors starting from LBA 1...
Successfully read 2 sectors (1024 bytes)
Hex dump of first sector (first 64 bytes):
  0x000: 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00  | ................
  ...

=== Demo 4: Write/Verify Test ===
Skipping destructive write test for safety.

=== Demo 5: Read Performance Test ===
Read test completed

=== Demo 6: LBA48 Support Test ===
Device: QEMU HARDDISK
LBA48 Supported: Yes
LBA28 auto-selection working correctly

=== Demo 7: Async I/O Test ===
Async I/O initialized
Async read completed successfully
All async reads completed

=== ATA Demo Complete ===
```

---

## 到这里我们完成了什么

恭喜！我们已经完成了一个完整的 ATA 磁盘驱动实现。让我们回顾一下这个阶段实现了什么：

### 核心功能
1. ✅ 设备检测与 IDENTIFY 命令
2. ✅ PIO 模式读写
3. ✅ LBA/LBA48 寻址支持
4. ✅ 异步 I/O 与中断驱动
5. ✅ 完整的演示程序

### 技术亮点
1. **硬件协议理解** - ATA 协议的时序和命令
2. **中断驱动** - IRQ 处理和异步队列
3. **错误处理** - 超时、错误码、状态检查
4. **模块化设计** - 公共 API 与内部实现分离

### 下一步建议

如果你想要继续完善这个驱动，可以考虑：

1. **DMA 支持** - 实现 Bus Master DMA
2. **缓存管理** - 添加扇区缓存提高性能
3. **多磁盘支持** - 实现 RAID 或磁盘调度
4. **SATA 支持** - 添加 AHCI 控制器支持
5. **NVMe 支持** - 现代高速存储接口

### 文件系统基础

有了磁盘驱动，下一步就可以实现文件系统了。推荐从简单的文件系统开始：
1. **FAT32** - 广泛支持，实现简单
2. **ext2** - Linux 标准文件系统
3. **自定义文件系统** - 针对操作系统优化

---

## 结语

写到这里，我们的 ATA 驱动教程就告一段落了。从零开始实现一个磁盘驱动确实是一段充满挑战的旅程，但同时也是一段非常充实的学习经历。

希望通过这些文章，你不仅学会了如何写 ATA 驱动，更重要的是理解了硬件驱动开发的思维方式。这种思维方式在任何底层开发中都是通用的。

如果你在实现过程中遇到问题，不要气馁。驱动开发本质上就是与硬件打交道，调试和排错是家常便饭。仔细检查每一个寄存器操作，严格遵守协议规范，问题总能解决的。

祝你在操作系统开发的道路上越走越远！

---

<div align="center">

## 文档导航

[← 异步 I/O 与中断驱动](06_异步I_O与中断驱动.md)  | [用户进程支持 →](../23_usr_proc/01_为什么需要用户进程.md)

</div>
