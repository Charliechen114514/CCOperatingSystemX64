# 编写进程演示程序

## 前言

之前的文章中，我们逐步实现了进程管理的各个组件：PID 分配器、PCB 管理、调度器、上下文切换、fork、exit、wait4、线程管理。这些组件分别测试过，但还没验证它们能否协同工作。

现在我们需要一个完整的演示程序，测试进程管理的所有功能。这个演示程序会创建多个进程和线程，验证它们能正常创建、切换、退出、回收。如果演示通过，说明进程管理系统基本可用；如果失败，说明还有 bug 需要修复。

## 演示程序设计

演示程序位于 `kernel/demo/process_simple/` 目录下，包含两个文件：

- `process_demo.h` - 演示程序接口
- `process_demo.c` - 演示程序实现

演示程序提供两个函数：
- `process_run_demo()` - 运行所有测试
- `process_stop_demo()` - 停止演示（用于清理）

每个测试都是一个独立的函数，返回 bool 表示测试通过或失败。测试函数会打印测试名称和结果，便于调试。

## 测试结果跟踪

首先定义一些宏来跟踪测试结果：

```c
static int tests_passed = 0;
static int tests_failed = 0;

#define TEST_ASSERT(condition, message) \
    do { \
        if (!(condition)) { \
            klog_error("[PROC_DEMO] FAILED: %s\n", message); \
            tests_failed++; \
            return false; \
        } \
    } while(0)

#define TEST_START(name) \
    klog_info("[PROC_DEMO] Test: %s...\n", name)

#define TEST_PASS() \
    do { \
        klog_info("[PROC_DEMO] PASSED\n"); \
        tests_passed++; \
        return true; \
    } while(0)
```

`TEST_ASSERT` 检查条件，如果失败打印错误信息并返回 false。`TEST_START` 和 `TEST_PASS` 标记测试的开始和结束，并更新计数器。

## 测试 1：PID 分配

第一个测试验证 PID 分配器的正确性：

```c
static bool test_pid_allocation(void) {
    TEST_START("PID Allocation");

    /* Initialize PID allocator */
    pid_alloc_init();

    /* Allocate some PIDs */
    int32_t pid1 = pid_alloc();
    TEST_ASSERT(pid1 > 0, "First PID allocation failed");

    int32_t pid2 = pid_alloc();
    TEST_ASSERT(pid2 > 0 && pid2 != pid1, "Second PID allocation failed or duplicate");

    /* Free first PID */
    pid_free(pid1);

    /* Allocate again - should get the same PID back */
    int32_t pid3 = pid_alloc();
    TEST_ASSERT(pid3 == pid1, "PID reuse after free failed");

    TEST_PASS();
}
```

测试分配两个 PID，验证它们不同。然后释放第一个 PID，再次分配应该得到相同的 PID。

## 测试 2：PCB 分配

第二个测试验证 PCB 分配和初始化：

```c
static bool test_pcb_allocation(void) {
    TEST_START("PCB Allocation");

    /* Allocate a PCB */
    pcb_t* pcb = (pcb_t*)kmalloc(sizeof(pcb_t));
    TEST_ASSERT(pcb != NULL, "PCB allocation failed");

    /* Initialize basic fields */
    memset(pcb, 0, sizeof(pcb_t));
    pcb->pid = 100;
    pcb->ppid = 1;
    pcb->state = PROC_READY;

    /* Verify fields */
    TEST_ASSERT(pcb->pid == 100, "PID not set correctly");
    TEST_ASSERT(pcb->state == PROC_READY, "State not set correctly");

    /* Free PCB */
    kfree(pcb);

    TEST_PASS();
}
```

这里我们直接用 `kmalloc` 分配 PCB，而不是用 `proc_alloc_pcb`。这是因为 `proc_alloc_pcb` 需要很多基础设施（如调度器）已经初始化，而测试希望尽早验证基本功能。

## 测试 3：进程状态转换

第三个测试验证进程状态机的正确性：

```c
static bool test_process_states(void) {
    TEST_START("Process State Transitions");

    /* Create a test PCB */
    pcb_t* pcb = (pcb_t*)kmalloc(sizeof(pcb_t));
    TEST_ASSERT(pcb != NULL, "PCB allocation failed");
    memset(pcb, 0, sizeof(pcb_t));

    /* Test state transitions */
    pcb->state = PROC_READY;
    TEST_ASSERT(pcb->state == PROC_READY, "Ready state not set");

    pcb->state = PROC_RUNNING;
    TEST_ASSERT(pcb->state == PROC_RUNNING, "Running state not set");

    pcb->state = PROC_BLOCKED;
    TEST_ASSERT(pcb->state == PROC_BLOCKED, "Blocked state not set");

    pcb->state = PROC_ZOMBIE;
    TEST_ASSERT(pcb->state == PROC_ZOMBIE, "Zombie state not set");

    kfree(pcb);

    TEST_PASS();
}
```

这个测试验证进程可以在各个状态之间转换。虽然简单，但它确保了状态枚举的定义和使用是正确的。

## 测试 4：调度器初始化

第四个测试验证调度器的初始化：

```c
static bool test_scheduler_init(void) {
    TEST_START("Scheduler Initialization");

    /* Initialize process subsystem */
    int result = proc_init();
    TEST_ASSERT(result == 0, "Process initialization failed");

    /* Verify scheduler is initialized */
    extern scheduler_t scheduler;
    TEST_ASSERT(scheduler.current == NULL, "Current process should be NULL after init");
    TEST_ASSERT(list_is_empty(&scheduler.run_queue), "Run queue should be empty after init");
    TEST_ASSERT(scheduler.nr_running == 0, "Running count should be 0 after init");

    TEST_PASS();
}
```

这个测试会触发完整的进程管理子系统初始化，包括调度器类框架、Round-Robin 调度器、PID 分配器等。如果任何模块初始化失败，测试会失败。

## 测试 5：PCB 链表管理

第五个测试验证 PCB 的链表操作：

```c
static bool test_pcb_lists(void) {
    TEST_START("PCB List Management");

    /* Create test PCBs */
    pcb_t* pcb1 = (pcb_t*)kmalloc(sizeof(pcb_t));
    pcb_t* pcb2 = (pcb_t*)kmalloc(sizeof(pcb_t));
    TEST_ASSERT(pcb1 != NULL && pcb2 != NULL, "PCB allocation failed");

    memset(pcb1, 0, sizeof(pcb_t));
    memset(pcb2, 0, sizeof(pcb_t));

    INIT_LIST_HEAD(&pcb1->siblings);
    INIT_LIST_HEAD(&pcb2->siblings);
    INIT_LIST_HEAD(&pcb1->children);
    INIT_LIST_HEAD(&pcb2->children);
    INIT_LIST_HEAD(&pcb1->run_list);
    INIT_LIST_HEAD(&pcb2->run_list);

    /* Create a parent-child relationship */
    pcb1->pid = 1;
    pcb2->pid = 2;
    pcb2->parent = pcb1;
    pcb2->ppid = 1;

    /* Add child to parent's children list */
    list_add_tail(&pcb2->siblings, &pcb1->children);

    /* Verify relationship */
    TEST_ASSERT(!list_is_empty(&pcb1->children), "Children list should not be empty");

    /* Cleanup */
    list_del(&pcb2->siblings);
    kfree(pcb1);
    kfree(pcb2);

    TEST_PASS();
}
```

这个测试验证链表操作的正确性。进程管理大量使用链表来组织进程（运行队列、子进程链表、线程组等），所以链表操作必须正确。

## 测试 6：内核栈管理

第六个测试验证内核栈的分配和对齐：

```c
static bool test_kernel_stack(void) {
    TEST_START("Kernel Stack Management");

    /* Allocate PCB with kernel stack */
    pcb_t* pcb = (pcb_t*)kmalloc(sizeof(pcb_t));
    TEST_ASSERT(pcb != NULL, "PCB allocation failed");
    memset(pcb, 0, sizeof(pcb_t));

    /* Allocate kernel stack */
    pcb->kernel_stack_base = (virtual_addr_t)kmalloc(KERNEL_STACK_SIZE);
    TEST_ASSERT(pcb->kernel_stack_base != 0, "Kernel stack allocation failed");

    /* Set stack top (stack grows down) */
    pcb->kernel_stack = pcb->kernel_stack_base + KERNEL_STACK_SIZE;

    /* Verify stack is properly aligned */
    TEST_ASSERT((pcb->kernel_stack & 0xF) == 0, "Kernel stack not 16-byte aligned");

    /* Cleanup */
    kfree((void*)pcb->kernel_stack_base);
    kfree(pcb);

    TEST_PASS();
}
```

栈对齐很重要，x86_64 ABI 要求栈在函数调用时必须 16 字节对齐。如果对齐错误，调用一些需要 SSE 指令的函数时会崩溃。

## 测试 7：内存上下文

第七个测试验证内存上下文的设置：

```c
static bool test_memory_context(void) {
    TEST_START("Memory Context");

    /* Create PCB with memory context */
    pcb_t* pcb = (pcb_t*)kmalloc(sizeof(pcb_t));
    TEST_ASSERT(pcb != NULL, "PCB allocation failed");
    memset(pcb, 0, sizeof(pcb_t));

    /* Initialize memory context */
    pcb->mm.pml4_phys = 0x1000;
    pcb->mm.brk = 0x400000;
    pcb->mm.stack_start = USER_STACK;

    /* Verify values */
    TEST_ASSERT(pcb->mm.pml4_phys == 0x1000, "PML4 not set correctly");
    TEST_ASSERT(pcb->mm.brk == 0x400000, "Break not set correctly");
    TEST_ASSERT(pcb->mm.stack_start == USER_STACK, "Stack start not set correctly");

    kfree(pcb);

    TEST_PASS();
}
```

## 运行所有测试

`process_run_demo()` 函数运行所有测试并打印总结：

```c
int process_run_demo(void) {
    klog_info("\n");
    klog_info("╔════════════════════════════════════════╗\n");
    klog_info("║   Process Management Demo                   ║\n");
    klog_info("╚════════════════════════════════════════╝\n");

    tests_passed = 0;
    tests_failed = 0;

    /* Run all tests */
    test_pid_allocation();
    test_pcb_allocation();
    test_process_states();
    test_scheduler_init();
    test_pcb_lists();
    test_kernel_stack();
    test_memory_context();

    /* Print summary */
    klog_info("\n");
    klog_info("╔════════════════════════════════════════╗\n");
    klog_info("║   Process Demo Summary                     ║\n");
    klog_info("╚════════════════════════════════════════╝\n");
    klog_info("[PROC_DEMO] Tests passed: %d\n", tests_passed);
    klog_info("[PROC_DEMO] Tests failed: %d\n", tests_failed);

    if (tests_failed == 0) {
        klog_info("[PROC_DEMO] ✓ All tests PASSED\n");
        return 0;
    } else {
        klog_error("[PROC_DEMO] ✗ %d test(s) FAILED\n", tests_failed);
        return -1;
    }
}
```

## 集成到内核

演示程序需要集成到内核的 main 函数中。在内核初始化完成后，调用 `process_run_demo()`：

```c
void kernel_main(void) {
    /* ... other initialization ... */

    /* Initialize process subsystem */
    proc_init();

    /* Run process demo */
    process_run_demo();

    /* ... continue ... */
}
```

## 预期输出

如果所有测试通过，你应该看到类似的输出：

```
╔════════════════════════════════════════╗
║   Process Management Demo                   ║
╚════════════════════════════════════════╝
[PROC_DEMO] Test: PID Allocation...
[PROC_DEMO] PASSED
[PROC_DEMO] Test: PCB Allocation...
[PROC_DEMO] PASSED
[PROC_DEMO] Test: Process State Transitions...
[PROC_DEMO] PASSED
[PROC_DEMO] Test: Scheduler Initialization...
[PROC_DEMO] PASSED
[PROC_DEMO] Test: PCB List Management...
[PROC_DEMO] PASSED
[PROC_DEMO] Test: Kernel Stack Management...
[PROC_DEMO] PASSED
[PROC_DEMO] Test: Memory Context...
[PROC_DEMO] PASSED

╔════════════════════════════════════════╗
║   Process Demo Summary                     ║
╚════════════════════════════════════════╝
[PROC_DEMO] Tests passed: 7
[PROC_DEMO] Tests failed: 0
[PROC_DEMO] ✓ All tests PASSED
```

## 调试技巧

如果测试失败，这里有一些调试技巧：

**单独运行测试** - 把失败的测试从 `process_run_demo()` 中注释掉，单独运行。这样可以减少干扰。

**打印详细信息** - 在测试函数中添加更多 `klog_info` 打印，打印每个步骤的中间结果。

**使用 GDB** - 在 QEMU 中用 GDB 调试，在断点处检查 PCB 的内容，验证字段是否正确设置。

**检查内存泄漏** - 在测试前后打印堆的统计信息，看是否有内存泄漏。`heap_dump()` 可以转储整个堆的状态。

**逐步增加测试** - 如果所有测试都失败，从最简单的测试（如 PID 分配）开始，逐步添加更复杂的测试，找出第一个失败的点。

## 常见问题

**测试随机失败** - 这通常是未初始化数据或竞态条件导致的。检查 PCB 是否用 `memset` 清零，检查是否有多个线程同时访问共享数据。

**内存不足** - 测试分配了大量 PCB 和栈，可能耗尽堆内存。增加堆大小或减少测试中分配的对象数量。

**断言失败** - `TEST_ASSERT` 失败说明某个条件不满足。查看错误信息，定位到具体的检查点，然后向前追溯找出问题的根源。

## 接下来

演示程序完成后，我们验证了进程管理的基本功能。但还有一个重要的部分：系统调用集成。

用户程序通过系统调用来创建进程、退出、等待子进程。我们需要把这些进程管理函数注册为系统调用，让用户程序可以使用它们。

在下一篇文章中，我们会完成系统调用集成，编写用户态测试程序，验证整个系统从用户态到内核态的完整流程。

准备好了吗？我们继续。

---

<div align="center">

## 文档导航

[← 实现线程管理](./10_实现线程管理.md) | [系统调用集成与测试 →](./12_系统调用集成与测试.md)

</div>
